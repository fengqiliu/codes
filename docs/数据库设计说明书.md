# 医学影像AI集成平台 V2.0 数据库设计说明书

---

## 文档信息

| 项目名称 | 医学影像AI集成平台 V2.0 |
|---------|------------------------|
| 数据库类型 | PostgreSQL 16 |
| 文档版本 | V2.0 |
| 创建日期 | 2026-01-15 |

---

## 1. 数据库架构

### 1.1 数据库清单

| 数据库名称 | 用途 | 字符集 |
|-----------|------|-------|
| medical_ai | 主业务数据库 | UTF8 |
| medical_ai_log | 日志数据库 | UTF8 |

### 1.2 Schema设计

```
medical_ai (主库)
├── public (默认schema)
│   ├── 用户权限相关表
│   ├── AI模型相关表
│   ├── API接口相关表
│   └── 系统配置相关表
└── audit (审计schema)
    └── 操作日志表

medical_ai_log (日志库)
└── public
    ├── 调用日志表
    └── 性能指标表
```

---

## 2. ER图

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│    sys_user     │       │    sys_role     │       │ sys_permission  │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id              │───┐   │ id              │───┐   │ id              │
│ username        │   │   │ code            │   │   │ code            │
│ password        │   │   │ name            │   │   │ name            │
│ email           │   │   │ description     │   │   │ resource        │
│ status          │   │   │ status          │   │   │ action          │
│ tenant_id       │   │   └─────────────────┘   │   └─────────────────┘
└─────────────────┘   │           │              │           │
         │            │           ▼              │           ▼
         │            │   ┌─────────────────┐   │   ┌─────────────────┐
         │            └──▶│ sys_user_role   │   └──▶│sys_role_permission│
         │                ├─────────────────┤       ├─────────────────┤
         │                │ user_id         │       │ role_id         │
         │                │ role_id         │       │ permission_id   │
         │                └─────────────────┘       └─────────────────┘
         │
         │    ┌─────────────────┐       ┌─────────────────┐
         │    │   ai_model      │       │  ai_model_config│
         │    ├─────────────────┤       ├─────────────────┤
         │    │ id              │◀──────│ model_id        │
         │    │ model_id        │       │ config_key      │
         │    │ name            │       │ config_value    │
         │    │ type            │       └─────────────────┘
         │    │ version         │
         │    │ endpoint        │       ┌─────────────────┐
         │    │ status          │       │ai_model_version │
         │    │ tenant_id       │       ├─────────────────┤
         │    └─────────────────┘◀──────│ model_id        │
         │            │                 │ version         │
         │            │                 │ status          │
         │            │                 │ release_note    │
         │            │                 └─────────────────┘
         │            │
         │            ▼
         │    ┌─────────────────┐       ┌─────────────────┐
         │    │  api_endpoint   │       │  api_key        │
         │    ├─────────────────┤       ├─────────────────┤
         │    │ id              │       │ id              │
         │    │ path            │       │ key             │
         └───▶│ model_id        │       │ secret          │
              │ method          │◀──────│ api_endpoint_id │
              │ rate_limit      │       │ status          │
              │ tenant_id       │       │ user_id         │
              └─────────────────┘       └─────────────────┘
                      │
                      ▼
              ┌─────────────────┐
              │ invoke_log      │
              ├─────────────────┤
              │ id              │
              │ request_id      │
              │ model_id        │
              │ api_endpoint_id │
              │ status          │
              │ response_time   │
              │ created_at      │
              └─────────────────┘
```

---

## 3. 表结构设计

### 3.1 用户权限模块

#### 3.1.1 sys_user (用户表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| username | VARCHAR(50) | NOT NULL UNIQUE | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码(BCrypt加密) |
| email | VARCHAR(100) | NOT NULL | 邮箱 |
| phone | VARCHAR(20) | | 手机号 |
| real_name | VARCHAR(50) | | 真实姓名 |
| avatar | VARCHAR(255) | | 头像URL |
| status | SMALLINT | NOT NULL DEFAULT 1 | 状态:0禁用,1启用 |
| tenant_id | BIGINT | NOT NULL | 租户ID |
| last_login_at | TIMESTAMP | | 最后登录时间 |
| last_login_ip | VARCHAR(50) | | 最后登录IP |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |
| deleted_at | TIMESTAMP | | 删除时间(软删除) |

**索引：**
- idx_sys_user_username (username)
- idx_sys_user_tenant (tenant_id)
- idx_sys_user_status (status)

#### 3.1.2 sys_role (角色表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| code | VARCHAR(50) | NOT NULL UNIQUE | 角色编码 |
| name | VARCHAR(100) | NOT NULL | 角色名称 |
| description | TEXT | | 角色描述 |
| status | SMALLINT | NOT NULL DEFAULT 1 | 状态 |
| tenant_id | BIGINT | NOT NULL | 租户ID |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |

#### 3.1.3 sys_permission (权限表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| code | VARCHAR(100) | NOT NULL UNIQUE | 权限编码 |
| name | VARCHAR(100) | NOT NULL | 权限名称 |
| resource | VARCHAR(50) | NOT NULL | 资源类型 |
| action | VARCHAR(50) | NOT NULL | 操作类型 |
| description | TEXT | | 权限描述 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |

#### 3.1.4 sys_user_role (用户角色关联表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| user_id | BIGINT | NOT NULL | 用户ID |
| role_id | BIGINT | NOT NULL | 角色ID |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |

**约束：**
- UNIQUE (user_id, role_id)
- FK user_id -> sys_user(id)
- FK role_id -> sys_role(id)

### 3.2 AI模型模块

#### 3.2.1 ai_model (AI模型表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| model_id | VARCHAR(100) | NOT NULL UNIQUE | 模型唯一标识 |
| name | VARCHAR(200) | NOT NULL | 模型名称 |
| type | VARCHAR(50) | NOT NULL | 类型:LLM/IMAGE/VOICE/CUSTOM |
| description | TEXT | | 模型描述 |
| version | VARCHAR(50) | NOT NULL | 当前版本 |
| endpoint | VARCHAR(500) | NOT NULL | 服务端点URL |
| status | VARCHAR(20) | NOT NULL DEFAULT 'OFFLINE' | 状态:ONLINE/OFFLINE/MAINTENANCE |
| icon | VARCHAR(255) | | 模型图标 |
| tags | VARCHAR(500) | | 标签(逗号分隔) |
| tenant_id | BIGINT | NOT NULL | 租户ID |
| created_by | BIGINT | NOT NULL | 创建人 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |

**索引：**
- idx_ai_model_type (type)
- idx_ai_model_status (status)
- idx_ai_model_tenant (tenant_id)

#### 3.2.2 ai_model_config (模型配置表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| model_id | BIGINT | NOT NULL | 模型ID |
| config_key | VARCHAR(100) | NOT NULL | 配置键 |
| config_value | TEXT | NOT NULL | 配置值 |
| config_type | VARCHAR(20) | NOT NULL | 类型:STRING/NUMBER/JSON |
| description | VARCHAR(500) | | 配置说明 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |

**约束：**
- UNIQUE (model_id, config_key)
- FK model_id -> ai_model(id)

#### 3.2.3 ai_model_version (模型版本表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| model_id | BIGINT | NOT NULL | 模型ID |
| version | VARCHAR(50) | NOT NULL | 版本号 |
| status | VARCHAR(20) | NOT NULL | 状态:ACTIVE/INACTIVE/DEPRECATED |
| endpoint | VARCHAR(500) | | 版本端点(可选) |
| release_note | TEXT | | 发布说明 |
| is_default | BOOLEAN | NOT NULL DEFAULT FALSE | 是否默认版本 |
| published_at | TIMESTAMP | | 发布时间 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |

**约束：**
- UNIQUE (model_id, version)
- FK model_id -> ai_model(id)

### 3.3 API接口模块

#### 3.3.1 api_endpoint (API端点表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| path | VARCHAR(500) | NOT NULL | API路径 |
| method | VARCHAR(10) | NOT NULL | HTTP方法 |
| name | VARCHAR(200) | NOT NULL | 接口名称 |
| description | TEXT | | 接口描述 |
| model_id | BIGINT | | 关联模型ID |
| request_schema | JSONB | | 请求参数Schema |
| response_schema | JSONB | | 响应参数Schema |
| rate_limit | INTEGER | NOT NULL DEFAULT 100 | QPS限制 |
| timeout | INTEGER | NOT NULL DEFAULT 30000 | 超时时间(ms) |
| status | VARCHAR(20) | NOT NULL DEFAULT 'ACTIVE' | 状态 |
| version | VARCHAR(20) | NOT NULL DEFAULT 'v1' | API版本 |
| tenant_id | BIGINT | NOT NULL | 租户ID |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |

**索引：**
- idx_api_endpoint_path (path, method)
- idx_api_endpoint_model (model_id)

#### 3.3.2 api_key (API密钥表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| name | VARCHAR(100) | NOT NULL | 密钥名称 |
| key | VARCHAR(64) | NOT NULL UNIQUE | API Key |
| secret | VARCHAR(128) | NOT NULL | API Secret(加密存储) |
| user_id | BIGINT | NOT NULL | 所属用户 |
| status | VARCHAR(20) | NOT NULL DEFAULT 'ACTIVE' | 状态 |
| permissions | JSONB | | 权限配置 |
| rate_limit | INTEGER | | QPS限制覆盖 |
| expires_at | TIMESTAMP | | 过期时间 |
| last_used_at | TIMESTAMP | | 最后使用时间 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |

### 3.4 调用日志模块

#### 3.4.1 invoke_log (调用日志表 - 分区表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| request_id | VARCHAR(64) | NOT NULL | 请求ID |
| trace_id | VARCHAR(64) | | 链路追踪ID |
| model_id | BIGINT | NOT NULL | 模型ID |
| api_endpoint_id | BIGINT | | API端点ID |
| api_key_id | BIGINT | | API密钥ID |
| user_id | BIGINT | | 用户ID |
| request_body | JSONB | | 请求体 |
| response_body | JSONB | | 响应体 |
| status | VARCHAR(20) | NOT NULL | 状态:SUCCESS/FAILED/TIMEOUT |
| error_code | VARCHAR(20) | | 错误码 |
| error_message | TEXT | | 错误信息 |
| response_time | INTEGER | NOT NULL | 响应时间(ms) |
| client_ip | VARCHAR(50) | | 客户端IP |
| user_agent | VARCHAR(500) | | User-Agent |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |

**分区策略：** 按月分区
```sql
PARTITION BY RANGE (created_at);
-- invoke_log_2026_01, invoke_log_2026_02, ...
```

### 3.5 系统配置模块

#### 3.5.1 sys_config (系统配置表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| config_key | VARCHAR(100) | NOT NULL UNIQUE | 配置键 |
| config_value | TEXT | NOT NULL | 配置值 |
| config_type | VARCHAR(20) | NOT NULL | 类型 |
| description | VARCHAR(500) | | 描述 |
| is_system | BOOLEAN | NOT NULL DEFAULT FALSE | 是否系统配置 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |

#### 3.5.2 sys_tenant (租户表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| code | VARCHAR(50) | NOT NULL UNIQUE | 租户编码 |
| name | VARCHAR(200) | NOT NULL | 租户名称 |
| description | TEXT | | 描述 |
| logo | VARCHAR(255) | | Logo |
| contact_name | VARCHAR(50) | | 联系人 |
| contact_phone | VARCHAR(20) | | 联系电话 |
| contact_email | VARCHAR(100) | | 联系邮箱 |
| status | SMALLINT | NOT NULL DEFAULT 1 | 状态 |
| quota | JSONB | | 资源配额 |
| expires_at | TIMESTAMP | | 过期时间 |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 更新时间 |

### 3.6 审计日志模块

#### 3.6.1 audit_log (操作审计日志表)

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | BIGSERIAL | PRIMARY KEY | 主键ID |
| user_id | BIGINT | | 操作用户ID |
| username | VARCHAR(50) | | 用户名 |
| operation | VARCHAR(50) | NOT NULL | 操作类型 |
| module | VARCHAR(50) | NOT NULL | 功能模块 |
| description | TEXT | | 操作描述 |
| request_method | VARCHAR(10) | | 请求方法 |
| request_url | VARCHAR(500) | | 请求URL |
| request_params | JSONB | | 请求参数 |
| response_data | JSONB | | 响应数据 |
| status | VARCHAR(20) | NOT NULL | 操作状态 |
| error_msg | TEXT | | 错误信息 |
| ip | VARCHAR(50) | | 操作IP |
| user_agent | VARCHAR(500) | | User-Agent |
| duration | INTEGER | | 执行时长(ms) |
| created_at | TIMESTAMP | NOT NULL DEFAULT NOW() | 创建时间 |

---

## 4. 数据字典

### 4.1 枚举值定义

#### 模型类型 (ai_model.type)
| 值 | 说明 |
|---|------|
| LLM | 大语言模型 |
| IMAGE | 医学影像诊断 |
| VOICE | 语音识别/合成 |
| CUSTOM | 自定义模型 |

#### 模型状态 (ai_model.status)
| 值 | 说明 |
|---|------|
| ONLINE | 在线运行中 |
| OFFLINE | 已下线 |
| MAINTENANCE | 维护中 |

#### 用户状态 (sys_user.status)
| 值 | 说明 |
|---|------|
| 0 | 禁用 |
| 1 | 启用 |

#### 调用状态 (invoke_log.status)
| 值 | 说明 |
|---|------|
| SUCCESS | 调用成功 |
| FAILED | 调用失败 |
| TIMEOUT | 调用超时 |

---

## 5. 索引设计

### 5.1 索引策略

| 索引类型 | 使用场景 |
|---------|---------|
| B-Tree | 等值查询、范围查询、排序 |
| Hash | 仅等值查询 |
| GiST | 全文搜索、地理数据 |
| GIN | JSONB字段查询、数组查询 |

### 5.2 核心索引清单

```sql
-- 用户表索引
CREATE INDEX idx_sys_user_username ON sys_user(username);
CREATE INDEX idx_sys_user_tenant ON sys_user(tenant_id);
CREATE INDEX idx_sys_user_status ON sys_user(status) WHERE deleted_at IS NULL;

-- 模型表索引
CREATE INDEX idx_ai_model_type ON ai_model(type);
CREATE INDEX idx_ai_model_status ON ai_model(status);
CREATE INDEX idx_ai_model_tenant ON ai_model(tenant_id);
CREATE INDEX idx_ai_model_search ON ai_model USING gin(to_tsvector('chinese', name || ' ' || COALESCE(description, '')));

-- API端点索引
CREATE UNIQUE INDEX idx_api_endpoint_path_method ON api_endpoint(path, method, version);
CREATE INDEX idx_api_endpoint_model ON api_endpoint(model_id);

-- 调用日志索引
CREATE INDEX idx_invoke_log_request_id ON invoke_log(request_id);
CREATE INDEX idx_invoke_log_model ON invoke_log(model_id, created_at);
CREATE INDEX idx_invoke_log_user ON invoke_log(user_id, created_at);
CREATE INDEX idx_invoke_log_status ON invoke_log(status, created_at);
```

---

## 6. 分区设计

### 6.1 调用日志分区

```sql
-- 按月分区
CREATE TABLE invoke_log (
    id BIGSERIAL,
    request_id VARCHAR(64) NOT NULL,
    model_id BIGINT NOT NULL,
    status VARCHAR(20) NOT NULL,
    response_time INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    PRIMARY KEY (id, created_at)
) PARTITION BY RANGE (created_at);

-- 创建分区
CREATE TABLE invoke_log_2026_01 PARTITION OF invoke_log
    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');
    
CREATE TABLE invoke_log_2026_02 PARTITION OF invoke_log
    FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');
    
-- 自动创建分区的函数
CREATE OR REPLACE FUNCTION create_partition_if_not_exists(
    table_name TEXT,
    partition_date DATE
) RETURNS VOID AS $$
DECLARE
    partition_name TEXT;
    start_date DATE;
    end_date DATE;
BEGIN
    partition_name := table_name || '_' || to_char(partition_date, 'YYYY_MM');
    start_date := date_trunc('month', partition_date);
    end_date := start_date + INTERVAL '1 month';
    
    IF NOT EXISTS (SELECT 1 FROM pg_tables WHERE tablename = partition_name) THEN
        EXECUTE format(
            'CREATE TABLE %I PARTITION OF %I FOR VALUES FROM (%L) TO (%L)',
            partition_name, table_name, start_date, end_date
        );
    END IF;
END;
$$ LANGUAGE plpgsql;
```

---

## 7. 备份恢复策略

### 7.1 备份策略

| 备份类型 | 频率 | 保留期限 | 说明 |
|---------|------|---------|------|
| 全量备份 | 每日 | 30天 | pg_dump全库备份 |
| 增量备份 | 每小时 | 7天 | WAL归档 |
| 逻辑备份 | 每周 | 90天 | 关键表数据导出 |

### 7.2 恢复流程

```bash
# 1. 停止应用服务
# 2. 恢复全量备份
pg_restore -h localhost -U postgres -d medical_ai backup_2026_01_15.dump

# 3. 应用WAL日志
pg_wal_replay --target-time="2026-01-15 10:00:00"

# 4. 验证数据完整性
# 5. 启动应用服务
```

---

## 8. 性能优化建议

### 8.1 查询优化

1. **使用连接池**：配置HikariCP，最小连接数10，最大连接数50
2. **预编译语句**：使用PreparedStatement，设置statement_cache_size=100
3. **批量操作**：批量插入使用COPY或多值INSERT
4. **分页优化**：使用keyset分页替代OFFSET

### 8.2 配置优化

```ini
# postgresql.conf 关键参数
shared_buffers = 4GB                 # 内存的25%
effective_cache_size = 12GB          # 内存的75%
work_mem = 256MB                     # 复杂查询内存
maintenance_work_mem = 1GB           # 维护操作内存
random_page_cost = 1.1               # SSD优化
effective_io_concurrency = 200       # SSD并发
max_connections = 200                # 最大连接数
```
